{
  "name": "saturn-sc-gas-bids",
  "scripts": {
    "re": "npx hardhat clean && npx hardhat compile",
    "typecheck": "hardhat compile && tsc --noEmit",
    "coverage": "npx hardhat coverage",
    "start": "npx hardhat run dist/scripts/deploy.js --network localhost",
    "deploy": "npx hardhat run dist/scripts/deploy.js --network binanceTestnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-network-helpers": "^1.0.4",
    "@nomicfoundation/hardhat-toolbox": "^1.0.1",
    "@openzeppelin/hardhat-upgrades": "^1.20.0",
    "chai": "^4.3.6",
    "hardhat": "^2.10.2",
    "ts-node": "^10.9.1",
    "typescript": "^4.8.2"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.7.3",
    "@openzeppelin/contracts-upgradeable": "^4.7.3"
  }
}
